{{>sidenav}}
<div class="col-tog single-col">

    <div class="app">
        {{>topbar}}
        {{>edit-profile-header}}
        {{>edit-profile-top-admin-view}}
        {{>edit-bio}}
        {{>edit-titles}}

    </div>

</div>

<div class="col-tog double-col" hidden>

    <div class="side-col">
        <div class="side-col-scroll">
        {{>scroll-title-home}}
        </div>
    </div>

    <div class="app double">
        {{>topbar}}
        {{>edit-profile-header}}
        {{>edit-profile-top-admin-view}}
        {{>edit-bio}}
        {{>edit-titles}}

    </div>

</div>
